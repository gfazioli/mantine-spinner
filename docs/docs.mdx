import { InstallScript } from './components/InstallScript/InstallScript';
import * as demos from './demos';

## Installation

<InstallScript packages="@gfazioli/mantine-spinner" />

After installation import package styles at the root of your application:

```tsx
import '@gfazioli/mantine-spinner/styles.css';
```

You can import styles within a layer `@layer mantine-spinner` by importing `@gfazioli/mantine-spinner/styles.layer.css` file. 

```tsx
import '@gfazioli/mantine-spinner/styles.layer.css';
```



## Usage

The `Spinner` component allows you to create a spinner effect with any content.


<Demo data={demos.configurator} />

## Multiple

The `Spinner` component allows you to create a spinner effect with multiple items.

<Demo data={demos.multiple} />

### Vertical

The `Spinner` component allows you to create a spinner effect with multiple items in vertical direction.

<Demo data={demos.multipleVertical} />

## Examples

### Testimonials

Your customers feedback is important to you. Use Spinner to display your testimonials.

<Demo data={demos.feedbacks} />

### Branding

One of the most common use cases for `Spinner` is to create branding banners.

<Demo data={demos.example} />


